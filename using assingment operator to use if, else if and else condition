#include<bits/stdc++.h>
using namespace std;
int main()
{
	int a,add, subtract,multiply,divide;
	int b, c;
  cin >> c;
  cin>>a;
	cin>>b;
	
	if(c==1)
	{ 
		add=a+b;
		cout<<add;
	}
	else if(c==2)
	{
		subtract =a-b;
		cout<<subtract;
	}
	else{
		multiply =a*b;
		cout<<multiply;
	}
	return 0;
	
}
